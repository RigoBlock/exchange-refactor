import { shallow } from 'enzyme'
import {{name}} from './{{name}}'
import toJson from 'enzyme-to-json'

const props = 'changeme!'

describe('{{name}} component', () => {
  it('renders correctly', () => {
    expect(
      toJson(shallow(createComponentWithProps({{name}}, props)))
    ).toMatchSnapshot()
  })
})
